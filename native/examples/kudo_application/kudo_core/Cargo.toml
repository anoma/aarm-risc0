[package]
name = "kudo_core"
version = "0.1.0"
edition = "2021"

[dependencies]
aarm_core = { path = "../../../aarm_core" }
serde = { version = "1.0.197", default-features = false }
risc0-zkvm = { version = "^2.0.2", default-features = false, features = ['std', "unstable", "client"] }
rand = "0.8"
